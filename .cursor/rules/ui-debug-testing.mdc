---
description: Autonomous UI workflow testing with Playwright e2e (local and deployed), browser automation, and progress updates
alwaysApply: true
---

# Autonomous UI Workflow Validation Rule

For any feature build, enhancement, or debugging task that affects user workflows, the model must autonomously validate the full workflow from a user/UI perspective. Do not stop at code changes alone.

## 1. Mandatory UI testing

**Preferred: Playwright e2e** (project has `e2e/` and `playwright.config.ts`):

- **Local:** `npx playwright test` (uses baseURL `http://localhost:3000`; starts or reuses dev server).
- **Deployed:** Run against production so issues on the live site are caught:
  ```powershell
  $env:PLAYWRIGHT_BASE_URL = "https://groovegraph.s13.nyc"
  npx playwright test e2e/login-and-graph.spec.ts --project=deployed
  ```
- Add or extend specs in `e2e/*.spec.ts` for new workflows (login, nav, graph load, enrichment).
- For deployment or cross-origin fixes, **always run the deployed project**; local-only tests can miss CORS/credentials issues.

**Alternative: built-in browser** (`@browser` / cursor-ide-browser) when Playwright is not sufficient:

- Navigate to the real workflow entry point (e.g. `http://localhost:3000/enrichment` or `https://groovegraph.s13.nyc`).
- Snapshot before interaction; exercise full path (type/click/submit/wait); snapshot after critical steps.
- Verify visible user outcomes, not only API/network assumptions.

Do this repeatedly until the workflow works end-to-end or a hard blocker is identified with evidence.

## 2. Progress updates during attempts (mandatory)

While testing/fixing, provide concise status updates as attempts are made:

- State what attempt is running.
- Report observed outcome (pass/fail and user-visible behavior).
- State what will be tried next if it fails.

Do not wait for the user to intervene to continue the validation cycle.

## 3. Terminal session hygiene and port preflight

Before starting a local run expected on port `3000`, follow the **runtime hygiene** rule (see `port-3000-runtime-hygiene.mdc` and the `runtime-hygiene` subagent):

1. Check existing terminal sessions and running processes.
2. Stop or reuse stale/duplicate dev servers.
3. Ensure port `3000` is available before launching a new server (use `npm run dev`, which runs the port guard).
4. Start only the required process for the current validation run.

As work progresses:

- Keep terminal usage clean (avoid accumulating redundant server sessions).
- Shut down no-longer-needed local processes at natural checkpoints.

## 4. Evidence-first debugging and validation

When failures occur:

- Add/inspect runtime logs at route entry, pipeline stages, validation gates, and error boundaries.
- Use consistent log prefixes for traceability.
- Make fixes based on observed evidence.
- Re-run the same browser flow to confirm the fix from the user perspective.

## 5. Completion criteria

A task is not considered complete until:

- The end-to-end UI workflow has been exercised through browser automation.
- The user-visible result is confirmed working.
- The validation attempts and outcomes were communicated in status updates.
