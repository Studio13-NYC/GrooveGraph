# Music Ontology — coverage matrix (GrooveGraph v2)

**MO-first:** this matrix leads TypeQL design. Update it in parallel with `typedb/groovegraph-schema.tql`.

| MO class / property (IRI or label) | MVP | Later | Out of scope | Notes |
|-------------------------------------|-----|-------|----------------|-------|
| *(add rows as discovery proceeds)* | | | | |

**Reference:** upstream ontology per [`ontology-location.mc`](ontology-location.mc).
